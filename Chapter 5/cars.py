cars = ['audi', 'kia', 'bmw', 'toyota']

for car in cars:
    if car == 'bmw':
        print(car.upper())
    else:
        print(car.title())

# Every if statement is an expression that can be evaluated as Trueor False (called a conditional test)

# setting a variable equal to a value

vehicle = 'bmw'

# checking the equality of a variable

vehicle == 'bmw'

print(vehicle=='bmw')